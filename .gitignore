# dependencies / build artifacts
node_modules/
dist/
*.tsbuildinfo
*.log

# Analysis target projects: only the small demo fixtures are committed.
# Real targets (including large internal apps) stay local and are never published.
/.repo/*
!/.repo/sample-shop-react/
!/.repo/sample-shop-nuxt/

# Analysis outputs (graphs / join / screens JSON)
/json/*.json

# OS / editor
.DS_Store
.idea/
.vscode/
