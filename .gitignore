# dependencies / build artifacts
node_modules/
dist/
*.tsbuildinfo
*.log

# Analysis target projects stay local and are never published — including the
# demo fixtures. The whole checkout area is ignored.
/.repo/

# Analysis outputs (graphs / join / screens JSON)
/json/*.json

# Local pipeline config (machine-specific paths). The example stays tracked.
/flowmap.config
/flowmap.env
/.flowmaprc

# OS / editor
.DS_Store
.idea/
.vscode/
