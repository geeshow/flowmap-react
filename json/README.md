Analysis outputs (graphs + join files). Generated by ts-analyzer.

Layout — one directory per service (project root) under `frontend/`:

```
frontend/
  _manifest.json                 # catalogue of all services (paths are <service>/<file>)
  <service>/
    graph.json                   # call graph
    graph.screens.json           # screen/wireframe data
    graph.join.json              # frontend↔backend join
    graph.impact.json            # per-PR change impact (index)
    graph.impact/<n>.json        # heavy per-PR impact shards
```

`<service>` is the sanitized project-root path; `graph` is the configured `NAME`
base (`flowmap.config`). Regenerate with `./flowmap pipeline` (or the staged
`scripts/pipeline/*.sh`).
