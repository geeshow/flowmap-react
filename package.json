{
  "name": "flowmap-react-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-root launcher for the flowmap-react analyzer (ts-analyzer).",
  "scripts": {
    "build": "npm --prefix ts-analyzer install --no-audit --no-fund && npm --prefix ts-analyzer run build",
    "flowmap": "node ts-analyzer/dist/cli.js",
    "analyze": "node ts-analyzer/dist/cli.js analyze",
    "screens": "node ts-analyzer/dist/cli.js screens",
    "join": "node ts-analyzer/dist/cli.js join",
    "test": "npm --prefix ts-analyzer test"
  }
}
